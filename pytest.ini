[pytest]
testpaths = tests
# Makes "src" importable regardless of the current working directory, so
# "pytest tests/" works from anywhere. Without this the suite only passed when
# pytest happened to be invoked as "python -m pytest" from the repository root.
pythonpath = .
